import { Box } from '@chakra-ui/react';

const HeaderGallery = () => {
  return (
    <Box as="div" height={180}>
      {/*
        {['/cover/1-1.jpeg', '/cover/1-2.jpeg'].map(value => (
          <HeaderGalleryElement key={value} url={value} />
        ))}
      */}
    </Box>
  );
};

export default HeaderGallery;
